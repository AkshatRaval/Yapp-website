import { Sora, Inter } from "next/font/google";

export const sora = Sora({
  subsets: ["latin"],
  variable: "--font-sora",
});

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});